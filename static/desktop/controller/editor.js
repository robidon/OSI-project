// задел на будущее. надо будет рефакторить основной контроллер. сюда хочется вынести все вещи, связанные с редактированием в таблице
var Desktop_Controller_Editor = Controller.extend({
    //Desktop_Controller_File.superclass.constructor.apply(this, []);
    initialize:function () {
        var T = this;
        this.inherit();
        
    }
});
//extend(Desktop_Controller_Editor, Controller);